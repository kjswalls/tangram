# React Router 8 needs Node >= 22.22 (docs/STACK.md §2.3), and the failure mode on
# an older runtime is a build image that dies mid-install with an unrelated-looking
# error. `engines` is documentation unless this is on; there is no CI to catch it.
engine-strict=true
